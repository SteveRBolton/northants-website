<?php

/**
 * @file
 * Settings provided by custom modules built for the Northants project.
 */

$settings['frontend_uri'] = !empty($settings['frontend_uri']) ? $settings['frontend_uri'] : 'https://localhost:3000';
